# Unreal Engine regenerated artifacts (never commit)
Binaries/
DerivedDataCache/
Intermediate/
Saved/
Build/

# Template download cache (the 920 MB zip is not project source)
downloaded/

# World-building asset packs — kept on disk, not project source (the user's call, 2026-09-24)
Content/SD_Art/
Content/STF/

# Fab / vault download cache (the raw downloads, not the imported assets)
Content/VaultCache/

# Visual Studio / IDE
.vs/
.vscode/
*.sln
*.slnx
*.suo
*.user
*.opensdf
*.sdf
*.opendb
*.db

# OS junk
Thumbs.db
.DS_Store
desktop.ini
