// Copyright Prosperitocracy. All Rights Reserved.

#include "ProsperitocracyClass.h"

#include UE_INLINE_GENERATED_CPP_BY_NAME(ProsperitocracyClass)

// What a class IS lives in the asset — its name, its colour, its three loadouts, its ability roster
// and its access rules — so there is no logic here to keep in step with it. What reads those fields
// (the picker, and the access check in the slot path) is built on top, per ROADMAP section 2.
